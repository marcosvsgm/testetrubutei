# ================================
# DOCKERFILE DEPRECADO
# ================================
# Este Dockerfile não é mais utilizado.
# Use: backend/Dockerfile
# 
# A estrutura foi consolidada para evitar duplicação.
# Este arquivo será removido em versões futuras.
# ================================

FROM php:8.2-fpm-alpine

LABEL deprecated="true"
LABEL replacement="backend/Dockerfile"

RUN echo "⚠️  Este Dockerfile está deprecado. Use backend/Dockerfile" && exit 1
